# UFO ローター制御CSV 自動生成プロジェクト — 引き継ぎ資料

> このファイルは、クラウド版 Claude Code での検討内容を、**自宅PC（RTX 3080 Ti）上の Claude Code** に引き継ぐためのものです。
> これを読めば、これまでの分析・設計・検証結果を踏まえて「意味層（ASR + LLM）」の実装から続けられます。

---

## 0. プロジェクトのゴール

**音声ファイル（ASMR等）を解析し、Vorze の成人向けローターデバイス「UFO TW / UFO SA」を音声に同期して動かすCSVスクリプトを自動生成する。**

最重要要件:
- 単純な音量トリガーは NG（**普通の会話でもローターが動いてしまう**）。
- **吐息・ささやき・責めセリフ・擦過擬音（かりかり/こりこり/くるくる 等）・耳への吐息のタイミング**で動かしたい。
- セリフの「意図」まで汲み取るために、**音声→ASR→言語モデルで意図検出**する「意味層」を実装したい ← **今からPCでやるのはここ**。

---

## 1. これまでに判明したこと（確定事項）

### 1-1. 出力CSVフォーマット（Vorze UFO）
実サンプル3組（音声 .m4a ＋ 対応CSV）を解析して校正済み。

| 形式 | 列構成 | 例 |
|------|--------|----|
| **UFO TW**（2ローター） | `time, dirL, speedL, dirR, speedR` | `2413,1,15,1,15` |
| **UFO SA**（1ローター） | `time, dir, speed` | `41,1,10` |

- **time 単位 = 0.1秒（デシ秒）**。校正根拠: 3ファイルとも `max_time × 0.1 ≒ 音声の尺` で一致。
  - Track_3(UFOTW): max_time 8268 → 826.8s（実尺 833s）
  - 02(UFO SA): max_time 11214 → 1121s（実尺 1161s）
  - 03(UFO SA): max_time 6654 → 665s（実尺 707s）
  - ※スクリプトは曲末尾の無音/アウトロ手前で終わる傾向。
- **dir** ∈ {0,1}（回転方向）, **speed** ∈ {0..100}（観測最大は概ね90, 0=停止）。
- **行は値が変化したフレームのみ出力**（毎フレームではない）。先頭に停止行 `0,0,0[,0,0]`、末尾にも停止行を付与。

### 1-2. 音量↔速度の相関（実測）
元CSVは作者の手作業も入るが、基本は**音量が大きいほど速度が上がる**。
- 例: RMS −0.8dB → speed 13 / −15.9dB → speed 5（Track_3 の rotorB セクション）。

### 1-3. 声と吐息は音響的に分離できる（実測・重要）
ASMR音声（RJ01565596_03、5分地点・60秒）を librosa で分析:

| 特徴量 | 声(会話) | 吐息/ささやき |
|---|---|---|
| スペクトル重心 | 672 Hz | 1510 Hz |
| ゼロ交差率(ZCR) | 0.039 | 0.123 |
| 有声確率(pyin F0) | 0.77 | 0.011 |
| スペクトル平坦度 | ~0 | 0.020 |

**FFTを使わない軽量特徴量**でも同等に分離可能（端末内/軽量実装向け）:
| 特徴量 | 声 | 吐息 |
|---|---|---|
| 周期性(自己相関ピーク) | 0.83 | 0.17 |
| 高域エネルギー比(>2kHz) | 0.07 | 0.17 |
| ZCR | 0.10 | 0.22 |

→ **周期性(autocorr) + 高域比 + ZCR** の3つで声/吐息を分離できる。

---

## 2. 既に実装・検証済みの「音響層」アルゴリズム（要移植）

クラウド版では単一HTMLのブラウザアプリとして実装し、実音声で検証済み（**会話=平均speed31 に抑制 / 吐息=平均speed62 で反応**）。
PCではこれを Python に移植して土台にする想定。以下が完全な仕様。

### 2-1. 前処理
- 音声を**モノラル化 → 8kHz にダウンサンプル**（特徴抽出用。アンチエイリアス1次LPF後にストライド）。
- ステレオは L/R 各chも8kHzに（バイノーラル/ステレオ割当用）。

### 2-2. フレーム特徴（hop=0.1秒, window=0.12秒）
各フレームで:
- `db` = 20*log10(RMS)
- `hf` = RMS(highpass 2kHz) / RMS（高域比）。highpassは `x - onepoleLP(x,2000)`。
- `zc` = ゼロ交差率（平均減算後）
- `per` = **周期性**: ラグ範囲 `[sr/400 .. sr/70]`（=70〜400Hz）で正規化自己相関の最大値
- `eL, eR` = L/R chのフレームRMS（ステレオ時）

### 2-3. 刺激エンベロープ生成（コンテンツ連動）
```
loud   = clamp01((db - floor)/span)         # floor = (98パーセンタイルdb) - dyn(36dB)
speechW= clamp01((per-0.4)/0.35) * clamp01((0.5-hf)/0.4)   # 有声=会話らしさ 0..1
u      = clamp01((0.4-per)/0.3)             # 無声らしさ
hi     = max(clamp01((hf-0.08)/0.18), clamp01((zc-0.1)/0.2))
breathW= u * hi * (loud>0.04)               # 吐息/ささやき/摩擦音らしさ 0..1

# 擦過オンセット（かりかり等の反復摩擦音のアクセント）
scrub[i] = max(0, hf[i]*loud[i] - hf[i-1]*loud[i-1]) * clamp01((0.4-per[i])/0.3)

# 刺激量
speechDrive = loud * (1 - suppress*speechW)        # suppress(既定0.9)で会話を抑制
breathDrive = breathSens(1.2) * breathW * (0.4+0.6*loud)
scrubKick   = scrubSens(1.0) * min(1, scrub/scrubTop) * 0.6     # scrubTop=90%ile
stim = min(1, max(speechDrive, breathDrive) + scrubKick)
```

### 2-4. 速度・方向への変換
```
speed = pow(stim, gamma=0.7) * maxSpeed(90)
speed = EMA平滑(smooth=0.4)
speed = round(speed/step(5))*step ; if speed<gate(6): 0 ; clamp 0..maxSpeed
dir: 'scrub'モード → scrub/scrubTop>0.5 のとき反転（最短2フレーム間隔）
     'alternate' → alt秒ごと反転 / 'fixed' → 固定
```

### 2-5. UFO TW の2ローター割当
- **binaural（推奨）**: `bias=(eL-eR)/(eL+eR)` → 左ローター重み`(1+bias)/2`, 右`(1-bias)/2` → 耳の左右に追従。
- **stereo**: L/R各chのエネルギー比で配分。
- **bands**: 高域比hfで低音→左/高音→右に擬似分割。
- **mirror**: 左右同一。

### 2-6. CSV出力
- 0.1秒=1フレーム=time1単位。値が変化した行だけ出力。先頭/末尾に停止行。

> ⚠️ ブラウザ版 `index.html` / `README.md` はこのコミットで**リポジトリから削除済み**。
> アルゴリズムはこの章に完全に保存してあるので、Pythonへ新規移植すればよい（JSのDOM/Web Audio部分は不要）。

---

## 3. これからPCで作る「意味層」（ASR + LLM）

### 3-1. ハードウェア
- **RTX 3080 Ti（12GB VRAM）** → Whisper large-v3 が余裕で載る。7B〜14BのローカルLLM(Q4)も可。

### 3-2. 推奨スタック
- **ASR**: [`WhisperX`](https://github.com/m-bain/whisperX)（large-v3）= 単語レベルのタイムスタンプ＋強制アラインメント。日本語対応。
  - 代替: `faster-whisper`(CTranslate2)。話者分離が要るなら WhisperX の diarization。
- **（任意）音源分離**: `Demucs` で声を分離 → ASR精度向上。**ただし吐息/擬音は分離で消えやすい**ので「文字起こし専用」に使い、音響層の解析は原音で行うこと。
- **ローカルLLM**: `Ollama` + `qwen2.5:14b-instruct`（or 7b）/ `llama3.1:8b` 等で**意図タグ付け**。
- **音響層**: `librosa` / `numpy` / `soundfile`。m4aデコードは `ffmpeg`（または `imageio-ffmpeg` 同梱バイナリ）。

### 3-3. パイプライン設計（ハイブリッド）
**正確な“瞬間”のタイミングは音響層、“区間の意図”は意味層** が役割分担の肝。

```
1) 音響層: 第2章を Python 移植 → 0.1秒ごとの stim / dir（精密タイミング）
2) ASR (WhisperX large-v3): 原音 or 分離音 → 単語レベル transcript + 時刻
3) LLM 意図タグ付け:
   transcript を時系列で渡し、区間を分類:
     - 通常会話 / 地のナレーション      → 抑制(weight↓)
     - 責めセリフ（焦らし・命令・煽り）  → boost(weight↑)
     - 擬音の発話（かりかり/こりこり/くるくる/れろれろ 等） → 強boost＋方向反転トリガ
     - 吐息・耳責めの描写                → boost
   出力: [{start, end, mode, weight}] の「モード重みタイムライン」
4) マージ:
   final_stim(t) = acoustic_stim(t) * mode_weight(t)
   擬音の“語”の時刻には方向反転＆速度キックを上乗せ（語タイミング≒動作タイミング）
5) Vorze CSV 書き出し（TW=5列 / SA=3列, 0.1秒単位, 変化時のみ）
```

### 3-4. 既知の注意点（現実解）
- ささやき/吐息混じりの日本語はASR精度が落ちる。**意味層は“区間の重み付け”に使い、フレーム精度のタイミングは音響層に委ねる**こと。
- 単語タイムスタンプは±0.3秒程度ずれる前提で、擬音トリガは音響層の scrub オンセットと**AND/近傍マージ**して精度を補完する。
- LLM出力は必ず JSON スキーマ（start/end/mode/weight）で強制し、検証する。

---

## 4. サンプルデータ（PCに用意が必要）

クラウド環境のアップロードは引き継げないため、**以下をPC側に置くこと**（チューニング/検証に必須）:

| 音声(.m4a) | 対応CSV | 形式 | 実尺 |
|---|---|---|---|
| RJ01424137 Track_3 | `..._UFOTW.csv` (2052行, max_time 8268) | UFO TW | 13:53 (833s) |
| RJ01565596 02 | `02..._UFO.csv` (3725行, max_time 11214) | UFO SA | 19:21 (1161s) |
| RJ01565596 03 | `03..._UFO.csv` (1423行, max_time 6654) | UFO SA | 11:47 (707s) |

CSVは「正解の動き」の参考。音響層/意味層の出力を元CSVと比較してパラメータを追い込む。

---

## 5. 次のステップ（TODOチェックリスト）

- [ ] PCに Python 環境構築（ffmpeg, librosa, numpy, soundfile, WhisperX, Ollama）
- [ ] 第2章の音響層を Python に移植（`acoustic.py`）。元CSVと出力を比較して検証
- [ ] WhisperX large-v3 で日本語ASR（単語タイムスタンプ取得）
- [ ] （任意）Demucsで声分離を試し、ASR精度を比較
- [ ] LLM意図タグ付け（プロンプト＋JSONスキーマ）。擬音/責め/会話/吐息の区間重み出力
- [ ] 音響層×意味層のマージ＆Vorze CSV書き出し（`generate.py`、TW/SA対応）
- [ ] サンプル音声で生成 → 元CSVと比較、パラメータ調整
- [ ] （任意）Vorze実機 or プレイヤーで動作確認

---

## 6. 参考メモ
- これは DLsite 等の成人向けASMR作品付属の Vorze デバイス制御スクリプトと同フォーマット。
- 速度0-100 / 方向0-1 は Vorze 仕様準拠。
- クラウド環境では `imageio-ffmpeg` 同梱の ffmpeg で m4a をデコードした（`apt` のffmpegは取得失敗した）。PCでは普通に ffmpeg を入れればよい。

---

*引き継ぎ元: クラウド版 Claude Code セッション。リポジトリ: OkeanosKing/testGame（このMDが唯一のファイル）。*
